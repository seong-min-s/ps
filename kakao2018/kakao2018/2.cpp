#include<iostream>
#include<string>
#include<cstringt.h>
#include<algorithm>
#include<cmath>
using namespace std;
int main(void)
{
	string str;
	int sum = 0;
	cin >> str;
	const char* temp = str.c_str();

	score = atoi(temp[0]);
	for (int i = 0; i < 3; i++)
	{
		int score = atoi(it);
		
		do {
			if (*it == 'S') {
				score =
			}
		} while (!isdigit(*it));
	}
	return 0;
}